export type LoginRequest = {
    username: string;
    password: string;
}
export type RegisterRequest = {
    name: string;
    email: string;
    mobile: string;
    password: string;
    user_type: string;
}

